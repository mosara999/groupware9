db:
  driverClassName: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
  url: "jdbc:log4jdbc:mariadb://localhost/groupware9?useUnicode=true&characterEncoding=UTF-8"
  username: root
  password: gujc1004

server:
  servlet:
    context-path: /groupware9
    encoding:
      charset: UTF-8
      force: true
    session:
      timeout: 30m

spring:
  thymeleaf:
    # Only these view names are served by Thymeleaf; every other view name falls through to the
    # JSP InternalResourceViewResolver (applicationContext.xml) while the JSP->Thymeleaf migration
    # is in progress. Add a view name here as each screen gets converted.
    view-names: member/memberForm,board/BoardList,board/BoardRead,board/BoardReadAjax4Reply
