<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
        http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.0.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
        http://www.springframework.org/schema/jee http://www.springframework.org/schema/jee/spring-jee-3.0.xsd
        http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.0.xsd">

    <context:annotation-config/>

    <!-- tx:annotation-driven removed: it registers a shared "internalTransactionAdvisor" bean
         under the same name Spring Boot's TransactionAutoConfiguration uses (which activates
         automatically once a PlatformTransactionManager bean like txManager below exists),
         causing a bean-definition-override startup failure. Nothing in this codebase uses
         @Transactional anyway (writes use manual transactions, see CLAUDE.md), so Boot's
         auto-configured equivalent is a strict improvement, not a functional change. -->

    <!-- DB connection settings live in classpath:config/application.yml, not here. -->
    <bean id="yamlProperties" class="org.springframework.beans.factory.config.YamlPropertiesFactoryBean">
        <property name="resources" value="classpath:config/application.yml"/>
    </bean>

    <context:property-placeholder properties-ref="yamlProperties"/>

    <!-- HikariCP. MariaDB Connector/J is JDBC4-compliant, so the pool validates borrowed
         connections via Connection.isValid() automatically — no connectionTestQuery needed.
         Unlike DBCP2's removeAbandonedTimeout (which force-reclaimed a stuck connection),
         leakDetectionThreshold only logs a warning when a connection is held past the
         threshold; it does not reclaim it. -->
    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="close"
		  p:driverClassName="${db.driverClassName}" p:jdbcUrl="${db.url}"
          p:username="${db.username}" p:password="${db.password}"
          p:poolName="groupware9-hikari"
          p:maximumPoolSize="30"
          p:minimumIdle="3"
          p:connectionTimeout="30000"
          p:leakDetectionThreshold="30000" />

	<!-- SqlSession setup for MyBatis Database Layer -->
	<bean id="sqlSession" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<property name="mapperLocations" value="classpath:/sql/*.xml" />
	</bean>

	<bean id="sqlSessionTemplate" class="org.mybatis.spring.SqlSessionTemplate">
		<constructor-arg index="0" ref="sqlSession"></constructor-arg>
    </bean>  
    
    <bean id="txManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource" />
    </bean>

	<tx:advice id="txAdvice" transaction-manager="txManager">
		<tx:attributes>
			<tx:method name="*" rollback-for="Exception"/>
		</tx:attributes>
	</tx:advice>

	<bean id="messageSource" class="org.springframework.context.support.ReloadableResourceBundleMessageSource" >
		<property name="defaultEncoding" value="UTF-8" />
		<property name="basenames">
			<list>
				<value>classpath:/message/message</value>
			</list>
		</property>
		<property name="fallbackToSystemLocale" value="false" />
		<property name="cacheSeconds" value="10"/>
	</bean>
 
	<bean id="localeResolver" class="org.springframework.web.servlet.i18n.SessionLocaleResolver" >
		<property name="defaultLocale" value="ko" />
	</bean>

    <bean id="messageSourceAccessor" class="org.springframework.context.support.MessageSourceAccessor">
        <constructor-arg ref="messageSource"/>
    </bean>
    <bean id="message" class="gu.common.LocaleMessage">
        <property name="messageSourceAccessor" ref="messageSourceAccessor"/>
    </bean> 
    		    
</beans>
